node_modules/
dist/
.env
.env.*
*.log
.vercel/
.env.local

data/testing.sqlite
data/testing.sqlite-*

data/sbc/source/

data/formulary/*.raw.json

data/providers/.tmp-*
data/providers/.state-*

# 21 MB, and scripts/extract-omnia-tiers.ts rebuilds it from a public url in
# under a minute, so it is a build artefact rather than a source of truth.
data/providers/horizon-omnia-tiers.json

